<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="@color/colorPrimary">

    <androidx.cardview.widget.CardView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_gravity="center"
        android:layout_margin="@dimen/margin18"
        android:elevation="@dimen/cardViewElevation"
        app:cardCornerRadius="@dimen/cardViewCornerRadius">

        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_margin="@dimen/margin18">

            <TextView
                android:id="@+id/tv_section_name"
                app:fontFamily="@font/roboto_light"
                style="@style/MainScreenTheme"
                android:textSize="@dimen/secondaryTextSize" />

            <TextView
                android:id="@+id/tv_publication_date"
                style="@style/MainScreenTheme"
                app:fontFamily="@font/roboto_light"
                android:layout_alignParentEnd="true"
                android:layout_alignParentRight="true"
                android:layout_toEndOf="@id/tv_section_name"
                android:layout_toRightOf="@id/tv_section_name"
                android:gravity="end"
                android:textSize="@dimen/secondaryTextSize" />

            <TextView
                android:id="@+id/tv_web_title"
                style="@style/MainScreenTheme"
                android:layout_below="@id/tv_section_name"
                android:layout_gravity="center"
                android:gravity="start"
                app:fontFamily="@font/roboto_medium"
                android:textSize="@dimen/primaryTextSize" />

        </RelativeLayout>

    </androidx.cardview.widget.CardView>

</LinearLayout>